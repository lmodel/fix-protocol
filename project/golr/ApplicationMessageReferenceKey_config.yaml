# metamodel_version: 1.11.0id: ApplicationMessageReferenceKey
schema_generating: true
description: One row of a "Key Fields for <area> Application Message References" table
  identifying the field whose value is copied into BusinessRejectRefID(379).
display_name: ApplicationMessageReferenceKey
document_category: ApplicationMessageReferenceKey
weight: 20
fields:
- id: category_label
  description: Category label as printed in the source table (free text; may include
    parenthesised sub-categories such as "Single General Order Handling").
  display_name: ApplicationMessageReferenceKey_category_label
- id: fix_message
  description: FIX message name with MsgType in parentheses, e.g. AllocationInstruction(35=J).
  display_name: ApplicationMessageReferenceKey_fix_message
- id: business_reject_ref_id_value
  description: Source field copied into BusinessRejectRefID(379) when the target message
    lacks its own reject. May enumerate several alternatives joined by "or".
  display_name: ApplicationMessageReferenceKey_business_reject_ref_id_value
